# Whop (from Whop Dashboard → Apps → Edit App)
WHOP_API_KEY=REPLACE_ME_SECRET               # NEVER expose to the client
NEXT_PUBLIC_WHOP_APP_ID=app_XXXXXXXXXXXX     # OK to be public

# OAuth redirect (must be registered in Whop Dashboard)
OAUTH_REDIRECT_URI=https://<your-vercel-domain>/api/oauth/callback

# Your app sessions (NOT Whop tokens)
SESSION_JWT_SECRET=REPLACE_WITH_LONG_RANDOM_SECRET

# CORS (comma-separated origins allowed for Unity testing)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
